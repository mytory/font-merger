# AGENTS

여러 폰트를 하나로 병합해 다국어 폰트를 만드는 Python CLI 및 PySide6 GUI 도구입니다.

## 커밋 및 출시 규칙

- 푸시는 절대 스스로 하지 않습니다. 사용자가 명시적으로 지시한 경우에만 처리합니다.
- 태그를 임의로 생성·이동하지 않습니다. 사용자가 명시적으로 지시한 경우에만 처리합니다.
- 출시 전에는 변경 사항과 버전을 검토하고, 버전·릴리스 범위에 관한 사용자 승인을 받습니다.

## 대화 및 수정 규칙

- 대화와 생각은 모두 한국어로 합니다.
- 사용자가 명시적으로 수정 의사를 밝히지 않으면 기본값은 설명만입니다.
- 관련 파일과 기존 패턴을 먼저 확인하고, 요청을 완전히 해결하는 최소 변경만 합니다.
- 추측하지 말고 코드·테스트·문서로 가정을 확인합니다.
- 변경 후 변경 범위에 맞는 검사를 실행하고, 변경 때문에 발생한 실패는 수정합니다.
- 반복 진단·수정 루프는 5회를 넘기지 않습니다. 더 필요하면 사용자에게 묻습니다.

## 공개 저장소와 배포물

- 입력 폰트, 빌드 산출물, 가상환경, 사용자 데이터, 비밀값은 Git에 추적하지 않습니다.
- GUI 배포물은 GitHub Actions에서 Windows x64, macOS Universal, Linux x64 대상으로 빌드합니다.
- PySide6/Qt는 LGPLv3/GPLv3 이중 라이선스입니다. 배포물에는 `LICENSE`와 `THIRD_PARTY_NOTICES.md`를 함께 포함합니다.
- PyInstaller는 번들 예외가 있는 GPL-2.0 도구입니다. 릴리스 워크플로는 고정된 버전의 의존성을 사용합니다.
- 코드 서명과 공증은 현재 제공하지 않습니다. README와 릴리스 노트에 OS 보안 경고 관련 안내를 유지합니다.

## 테스트와 검사

- 의존성 설치: `./setup.sh`
- CLI 도움말 확인: `./font-merger --help`
- GUI 구문 검사: `venv/bin/python3 -m py_compile _font_merger.py font_merger_gui.py`
- GUI 패키지 빌드는 GitHub Actions에서 각 대상 OS별로 검증합니다.
